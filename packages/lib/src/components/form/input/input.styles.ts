import { defineComponentStyles } from "../../../lib/component-styles";

export const inputStyles = defineComponentStyles({
    name: "input",
    css: "@g4rcez/components/input.css",
    variants: {},
    defaults: {},
    slots: [],
    dependencies: ["input-field"],
});
