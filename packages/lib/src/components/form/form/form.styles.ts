import { defineComponentStyles } from "../../../lib/component-styles";

export const formStyles = defineComponentStyles({
    name: "form",
    variants: {},
    defaults: {},
    slots: [],
    dependencies: [],
});
