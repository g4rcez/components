import { defineComponentStyles } from "../../lib/component-styles";

export const tableStyles = defineComponentStyles({
    name: "table",
    variants: {},
    defaults: {},
    slots: [],
    dependencies: ["table-root"],
});
