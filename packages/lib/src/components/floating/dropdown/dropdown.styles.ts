import { defineComponentStyles } from "../../../lib/component-styles";

export const dropdownStyles = defineComponentStyles({
    name: "dropdown",
    variants: {},
    css: "@g4rcez/components/dropdown.css",
    defaults: {},
    slots: ["arrow", "header", "title"],
    dependencies: [],
});
