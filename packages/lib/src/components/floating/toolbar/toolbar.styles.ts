import { defineComponentStyles } from "../../../lib/component-styles";

export const toolbarStyles = defineComponentStyles({
    name: "toolbar",
    variants: {},
    defaults: {},
    slots: [],
    dependencies: [],
});
