import { defineComponentStyles } from "../../lib/component-styles";

export const progressStyles = defineComponentStyles({
    name: "progress",
    variants: {},
    defaults: {},
    slots: ["indicator", "label"],
    dependencies: [],
});
