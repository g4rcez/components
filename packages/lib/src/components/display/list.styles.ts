import { defineComponentStyles } from "../../lib/component-styles";

export const listStyles = defineComponentStyles({
    name: "list",
    variants: {},
    defaults: {},
    slots: [],
    dependencies: [],
});
