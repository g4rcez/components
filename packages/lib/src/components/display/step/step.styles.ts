import { defineComponentStyles } from "../../../lib/component-styles";

export const stepStyles = defineComponentStyles({
    name: "step",
    variants: {},
    defaults: {},
    slots: [],
    dependencies: [],
});
