import { defineComponentStyles } from "../../../lib/component-styles";

export const spinnerStyles = defineComponentStyles({
    name: "spinner",
    variants: {},
    defaults: {},
    slots: ["container"],
    dependencies: [],
});
