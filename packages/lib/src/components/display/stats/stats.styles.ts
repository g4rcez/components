import { defineComponentStyles } from "../../../lib/component-styles";

export const statsStyles = defineComponentStyles({
    name: "stats",
    variants: {},
    defaults: {},
    slots: ["header", "icon", "icon-svg", "content", "title", "value", "footer"],
    dependencies: [],
});
