import { defineComponentStyles } from "../../lib/component-styles";

export const resizableStyles = defineComponentStyles({
    name: "resizable",
    variants: {},
    defaults: {},
    slots: [],
    dependencies: [],
});
