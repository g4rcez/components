import { defineComponentStyles } from "../../../lib/component-styles";

export const polymorphStyles = defineComponentStyles({
    name: "polymorph",
    variants: {},
    defaults: {},
    slots: [],
    dependencies: [],
});
