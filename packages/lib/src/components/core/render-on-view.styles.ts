import { defineComponentStyles } from "../../lib/component-styles";

export const renderOnViewStyles = defineComponentStyles({
    name: "render-on-view",
    variants: {},
    defaults: {},
    slots: [],
    dependencies: ["polymorph"],
});
