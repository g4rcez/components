import { defineComponentStyles } from "../../lib/component-styles";

export const headingStyles = defineComponentStyles({
    name: "heading",
    variants: {},
    defaults: {},
    slots: [],
    dependencies: ["polymorph"],
});
